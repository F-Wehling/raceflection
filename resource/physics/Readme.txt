This folder must contains all physics data that should be packaged.
By default all physics data is already contained in the scene files.

Create subfolders to indicate a package (foldername = packagename)