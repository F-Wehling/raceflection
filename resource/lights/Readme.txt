This folder must contains all light data that should be packaged.
By default all light data is already contained in the scene files.

Create subfolders to indicate a package (foldername = packagename)
