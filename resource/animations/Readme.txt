This folder must contains all animation data that should be packaged.
By default all animation data is already contained in the scene files.

Create subfolders to indicate a package (foldername = packagename)
