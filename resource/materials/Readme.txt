This folder must contains all material data that should be packaged.
By default all material data is already contained in the scene files.

Create subfolders to indicate a package (foldername = packagename)
