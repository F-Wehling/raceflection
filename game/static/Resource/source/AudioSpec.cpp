#include "AudioSpec.h"

BEGINNAMESPACE

const AudioSpec* AudioSpec::FromBuffer(const Byte* buffer){
	return nullptr;
}

uint32 AudioSpec::MemSize(const AudioSpec * spec)
{
	return uint32();
}

void AudioSpec::ToBuffer(const AudioSpec * res, Byte * buffer)
{
}

ENDNAMESPACE
