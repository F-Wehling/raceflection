set( Target_Name "AssetPipeline")

include(${cmake_root}/assimp.cmake)
include(${cmake_root}/freeimage.cmake)

if(UNIX)
    set(Target_Lib ${Target_Lib} -lstdc++fs)
endif()

include("../CMakeApplicationGeneration.cmake")
